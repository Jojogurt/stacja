.claude/
.DS_Store
node_modules/
server/node_modules/
.wrangler/
server/.wrangler/
*.log

# sekrety lokalne (token Cloudflare) — NIGDY do repo
.cf-token
server/.cf-token

# lokalny podgląd design-exportu (nie commitować)
_designref/

# lokalny design-export (źródło _designref) — nie commitować
STACJA - ekrany aplikacji.html
